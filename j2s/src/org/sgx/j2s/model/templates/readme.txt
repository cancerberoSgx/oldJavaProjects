ojbect temp`lates. a tempate represent the initial (default) state of an object. 

a used syntax can be like css:

$SELECTOR {	
	propertyName ":" propertyValue ";"
	...
}
where selector is a expression identifying the object in its containment tree (parentship)
 or in its hierrarchy tree (class), individually, etc
 
 
 here are some simple syntax implementations